% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_country_selection.R
\name{mod_country_selection_server}
\alias{mod_country_selection_server}
\title{Country / comparator selection module server}
\usage{
mod_country_selection_server(id, default_base = "GHA")
}
\arguments{
\item{id}{Module id (must match the UI).}

\item{default_base}{Character \code{unit_code} - must match the UI's
\code{default_base}; seeds the pre-Apply state so the dashboard renders
immediately.}
}
\value{
A named list of reactives: \code{base_unit} (scalar \code{unit_code}),
\code{comparison_units} (character vector, base removed), \code{threshold},
\code{year_range}, and \code{applied} (the Apply click count). \code{base_unit} ...
\code{year_range} hold the default selection until "Apply" is clicked, then
update only on each subsequent click.
}
\description{
Country / comparator selection module server
}
