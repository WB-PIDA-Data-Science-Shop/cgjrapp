% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_family.R
\name{def_quantiles}
\alias{def_quantiles}
\alias{def_quantiles_dyn}
\title{Classify indicators into Weak / Emerging / Strong within a comparison group}
\usage{
def_quantiles(
  ctf_long,
  base_unit,
  comparison_units,
  threshold = c("Default", "Terciles")
)

def_quantiles_dyn(
  ctf_long,
  base_unit,
  comparison_units,
  threshold = c("Default", "Terciles")
)
}
\arguments{
\item{ctf_long}{Long CTF tibble as returned by \code{\link[=filter_unit_data]{filter_unit_data()}} - one
row per \code{unit_code} x \code{variable} (x \code{year} for the dynamic variant), with
an \code{indicator} display label and a numeric \code{ctf} column.}

\item{base_unit}{Character vector of base \code{unit_code}(s) being benchmarked.}

\item{comparison_units}{Character vector of comparison \code{unit_code}s.}

\item{threshold}{\code{"Default"} (cut points at the 25th / 50th percentile) or
\code{"Terciles"} (33rd / 66th).}
}
\value{
A long tibble, one row per unit-indicator (unit-indicator-year for
\code{def_quantiles_dyn()}), with \code{dtf}, \code{dtt}, \code{q_lv_25}, \code{q_lv_75},
\code{q_cutoff1}, \code{q_cutoff2}, \code{status}, \code{nrank} added and the identifier /
taxonomy columns of \code{ctf_long} carried through.
}
\description{
Long-shape port of \code{cliarappak::def_quantiles()} (\code{def_quantiles_dyn()} for
the year-by-year variant). Given a base unit and a comparison set, ranks
every indicator \emph{within that selection only} and labels each unit's value.
}
\details{
Steps (identical to cliarappak):
\enumerate{
\item Drop indicators the base unit has no data for - \code{def_quantiles()} treats
\emph{any} \code{NA} as missing; \code{def_quantiles_dyn()} requires \emph{100\%} missing.
\item Restrict to base + comparison units and the surviving indicators.
\item Per indicator (per indicator-year for the dynamic variant): \code{dtt} =
\code{\link[dplyr:percent_rank]{dplyr::percent_rank()}} of \code{ctf}; \code{q_lv_25} / \code{q_lv_75}; the two
\verb{q_cutoff*} percentiles; the \code{status} label; \code{nrank} =
\code{dplyr::min_rank(-ctf)}. \code{ctf} is renamed \code{dtf}.
\item Drop indicators flat across the selection (25th == 75th percentile for
the base unit).
}
}
\seealso{
\code{\link[=compute_family_average_app]{compute_family_average_app()}}, \code{\link[=static_plot]{static_plot()}}, \code{\link[=static_plot_dyn]{static_plot_dyn()}}
}
