% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_data.R
\name{get_aggregate_data}
\alias{get_aggregate_data}
\title{Retrieve and combine regional and income-group aggregate CTF data}
\usage{
get_aggregate_data(
  cluster,
  subcluster,
  region_codes,
  income_groups,
  year_range
)
}
\arguments{
\item{cluster}{Character. A top-level key from \code{names(cgjrdata::ctfdata_list)},
e.g. \code{"institutional_environment"}.}

\item{subcluster}{Character. A second-level key from
\code{names(cgjrdata::ctfdata_list[[cluster]])}.}

\item{region_codes}{Character vector of \code{region_code} values to include
(e.g. \code{c("EAP", "SSA")}). Use \code{character(0)} to include none.}

\item{income_groups}{Character vector of \code{income_group} values to include
(e.g. \code{c("High income")}). Use \code{character(0)} to include none.}

\item{year_range}{An integer or numeric vector of length 2:
\code{c(min_year, max_year)}. Both bounds are inclusive.}
}
\value{
A tibble with columns \code{group_label}, \code{group_code}, \code{year}, \code{score},
\code{country_type}. Returns a zero-row tibble with those columns if both
\code{region_codes} and \code{income_groups} are empty or yield no rows.
}
\description{
Pulls precomputed CTF averages from \link[cgjrdata:regionctf_list]{cgjrdata::regionctf_list} and
\link[cgjrdata:incomectf_list]{cgjrdata::incomectf_list} for a given cluster/subcluster, filters by
selected codes and year range, and returns a normalised tibble ready to
be bound with country-level data for plotting.
}
