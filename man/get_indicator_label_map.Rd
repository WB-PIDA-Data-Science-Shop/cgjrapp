% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_data.R
\name{get_indicator_label_map}
\alias{get_indicator_label_map}
\title{Get a label map from indicator variable codes to human-readable names}
\usage{
get_indicator_label_map(indicators)
}
\arguments{
\item{indicators}{Character vector of indicator variable codes, e.g.
\code{c("wjp_rol_2", "vdem_core_v2x_pubcorr")}.}
}
\value{
A named character vector where names are the raw codes and values
are human-readable labels (from \code{metadata_tbl$var_name}, falling back to
the code itself when not found).
}
\description{
Looks up \code{cgjrdata::metadata_tbl} to map variable codes (column names in
subcluster tibbles) to their human-readable \code{var_name} labels. Variables
not found in metadata fall back to the raw code. Intended for use as DT
column headers in the detail module.
}
