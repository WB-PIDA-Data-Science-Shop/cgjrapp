% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_cluster_factory.R
\name{mod_cluster_server}
\alias{mod_cluster_server}
\title{Cluster detail tab server}
\usage{
mod_cluster_server(
  id,
  cluster_key,
  primary_iso,
  peer_isos,
  region_codes,
  income_groups,
  year_range,
  zone_type
)
}
\arguments{
\item{id}{Module namespace ID. Must match the \code{id} used in \code{\link[=mod_cluster_ui]{mod_cluster_ui()}}.}

\item{cluster_key}{Character. One of \code{names(cgjrdata::ctfdata_list)}.}

\item{primary_iso}{Reactive string — ISO3 of the focal country.}

\item{peer_isos}{Reactive character vector — peer ISO3 codes.}

\item{region_codes}{Reactive character vector — selected region codes.}

\item{income_groups}{Reactive character vector — selected income groups.}

\item{year_range}{Reactive integer vector of length 2.}

\item{zone_type}{Reactive string — one of \code{"abs_default"}, \code{"abs_tercile"},
\code{"rel_quartile"}, \code{"rel_tercile"}.}
}
\value{
Invoked for side effects (registers Shiny outputs).
}
\description{
Handles the server logic for one governance cluster tab. For each
subcluster, registers a \code{\link[plotly:plotly-shiny]{plotly::renderPlotly()}} output that:
}
\details{
\enumerate{
\item Filters country + peer rows via \code{\link[=filter_country_data]{filter_country_data()}}.
\item Retrieves aggregate rows via \code{\link[=get_aggregate_data]{get_aggregate_data()}}.
\item Computes annual thresholds via \code{\link[=get_annual_thresholds]{get_annual_thresholds()}} when a relative
threshold mode is selected.
\item Renders via \code{\link[=plot_cgjr_master]{plot_cgjr_master()}}.
}
}
