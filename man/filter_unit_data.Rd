% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_access.R
\name{filter_unit_data}
\alias{filter_unit_data}
\title{Slice indicator-level CTF rows out of \code{cgjr_ctf}}
\usage{
filter_unit_data(
  leaf = NULL,
  ctf_type = c("dynamic", "static"),
  units,
  year_range = NULL,
  even_years = FALSE,
  ctf = cgjrdata::cgjr_ctf
)
}
\arguments{
\item{leaf}{\code{NULL} (every leaf) or a character vector of leaf keys
(\code{dplyr::coalesce(sub_subcluster, subcluster)} from \code{cgjr_taxonomy}).}

\item{ctf_type}{One of \code{"dynamic"} or \code{"static"}.}

\item{units}{Character vector of \code{unit_code}s (ISO3 for countries, WB
\code{region_code} such as \code{"AFE"}, or an income-group slug such as
\code{"high_income"}). Countries, regions and income groups are equal citizens.}

\item{year_range}{Optional integer vector; its \code{range()} bounds the \code{year}
column. Ignored when \code{ctf_type == "static"} (static rows carry \code{year = NA}).}

\item{even_years}{Logical. When \code{TRUE} (and \code{ctf_type == "dynamic"}), keep
only even years - \code{cliarappak}'s rule for the dynamic benchmark, adopted
for \code{cgjrapp} (decision, 2026-09-10). Ignored for static.}

\item{ctf}{The long CTF tibble to filter. Defaults to \code{cgjrdata::cgjr_ctf};
overridable for tests.}
}
\value{
A tibble with the columns of \code{cgjr_ctf}
(\code{unit_level}, \code{unit_code}, \code{unit_name}, \code{year}, \code{ctf_type}, \code{cluster},
\code{subcluster}, \code{sub_subcluster}, \code{leaf}, \code{indicator}, \code{variable}, \code{ctf},
\code{n_inputs}, \code{n_inputs_obs}), filtered to the requested slice. Row order is
preserved from \code{cgjr_ctf}.
}
\description{
The single entry point the benchmarking dashboard uses to pull
indicator-grain closeness-to-frontier values. It only ever \strong{filters} the
long \code{cgjrdata::cgjr_ctf} tibble - it never recomputes a CTF value (the
upstream \code{cgjrdata} / \code{cliaretl} pipeline owns that; see the CGJR build
plan, non-negotiable constraint 4).
}
\details{
\code{leaf} is an argument, never baked into the function body. Pass one key for
a single leaf card, a vector for several, or \code{NULL} for every leaf in
\code{cgjr_ctf} (the Slice 1 generalisation - the downstream methodology
functions key on \code{leaf} themselves, so a whole-taxonomy slice rolls up
correctly).

Emptiness is \strong{not} an error. An empty \verb{(leaf, ctf_type)} pair - e.g.
\code{budget_cycle_and_fiscal_planning} for \code{ctf_type == "dynamic"}, which has no
dynamic-eligible indicators - contributes no rows. The caller renders "no
data available" for that case (build plan constraint 3).
}
\seealso{
\code{\link[=def_quantiles]{def_quantiles()}}, \code{\link[=compute_family_average_app]{compute_family_average_app()}}
}
