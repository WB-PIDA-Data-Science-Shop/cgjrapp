% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_data.R
\name{get_annual_thresholds}
\alias{get_annual_thresholds}
\title{Compute annual quantile thresholds from a reference dataset}
\usage{
get_annual_thresholds(data, y_var, year_range, probs = c(0.25, 0.5))
}
\arguments{
\item{data}{A tibble containing at least \code{year} (numeric) and the column
named by \code{y_var}. Pass the \strong{unfiltered} global reference dataset —
e.g. \code{cgjrdata::institutional_averages_tbl} for cluster-score plots, or
\code{cgjrdata::ctfdata_list[[cluster]][[subcluster]]} for indicator plots.}

\item{y_var}{Character. Name of the score/indicator column to quantile.}

\item{year_range}{Integer vector of length 2: \code{c(min_year, max_year)}.}

\item{probs}{Numeric vector of length 2: lower and upper quantile
probabilities. Defaults to \code{c(0.25, 0.50)} (quartile-based).}
}
\value{
A tibble with columns \code{year} (numeric), \code{q1} (lower threshold),
\code{q2} (upper threshold). One row per year in \code{year_range}.
}
\description{
For each year in \code{year_range}, computes two quantile cutoffs (\code{q1}, \code{q2})
from \code{y_var} across all rows (all countries), ignoring \code{NA}s. Returns a
one-row-per-year tibble that can be joined to plot data so zone boundaries
vary year-by-year.
}
