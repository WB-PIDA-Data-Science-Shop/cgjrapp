% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_family.R
\name{compute_family_average_app}
\alias{compute_family_average_app}
\title{Family / composite closeness-to-frontier score for a selection}
\usage{
compute_family_average_app(
  ctf_long,
  base_unit,
  comparison_units,
  type = c("static", "dynamic")
)
}
\arguments{
\item{ctf_long}{Long CTF tibble as returned by \code{\link[=filter_unit_data]{filter_unit_data()}} - one
row per \code{unit_code} x \code{variable} (x \code{year} for the dynamic variant), with
an \code{indicator} display label and a numeric \code{ctf} column.}

\item{base_unit}{Character vector of base \code{unit_code}(s) being benchmarked.}

\item{comparison_units}{Character vector of comparison \code{unit_code}s.}

\item{type}{\code{"static"} (one score per unit) or \code{"dynamic"} (one per
unit-year).}
}
\value{
A tibble with \code{leaf} (when \code{ctf_long} carries it), \code{unit_code} (and
\code{year} when \code{type == "dynamic"}), \code{score} (the mean, \code{NaN} collapsed to
\code{NA}), \code{n_inputs} (indicators that survived the screens) and
\code{n_inputs_obs} (of those, how many were observed for the unit). One row
per \code{leaf} x unit [x year] - pass a multi-leaf \code{ctf_long} to score the
whole taxonomy in one call.
}
\description{
Long-shape port of \code{cliarappak::compute_family_average_app()}. Applies the
app's missing-data and low-variance screens, then averages the surviving
indicators' \code{ctf} per unit (per unit-year when \code{type == "dynamic"}).
}
\details{
The missing-data screen is \code{cliarappak}'s: an indicator is dropped when the
base unit has \strong{any} \code{NA} for it, pooled across the whole selection window -
the same rule for static and dynamic (this is looser than
\code{\link[=def_quantiles_dyn]{def_quantiles_dyn()}}'s 100\%-missing rule; the difference is inherited from
\code{cliarappak} and asserted in the parity test).
}
\seealso{
\code{\link[=def_quantiles]{def_quantiles()}}
}
