% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_plotting.R
\name{plot_cgjr_master}
\alias{plot_cgjr_master}
\title{Master CTF benchmarking plot}
\usage{
plot_cgjr_master(
  data,
  y_var,
  primary_iso,
  year_range,
  threshold_mode = c("abs_quartile", "abs_tercile", "rel_quartile", "rel_tercile"),
  thresholds = NULL,
  show_members = FALSE,
  member_data = NULL
)
}
\arguments{
\item{data}{A tibble produced by combining \code{\link[=filter_country_data]{filter_country_data()}} and
(optionally) \code{\link[=get_aggregate_data]{get_aggregate_data()}}. Must contain columns: \code{year}
(numeric), \code{country_type} (one of \code{"primary"}, \code{"peer"}, \code{"region"},
\code{"income"}), and the column named in \code{y_var}. Country rows need
\code{country_code} and \code{country_name}; aggregate rows need \code{group_label}
and \code{group_code}.}

\item{y_var}{Character. Name of the column to map to the y-axis.}

\item{primary_iso}{Character. ISO3 code of the focal country.}

\item{year_range}{Integer vector of length 2: \code{c(min, max)}.}

\item{threshold_mode}{Character. One of:
\itemize{
\item \code{"abs_quartile"} — fixed 0.25 / 0.50 cutoffs (default).
\item \code{"abs_tercile"}  — fixed 1/3 / 2/3 cutoffs.
\item \code{"rel_quartile"} — annual 25th / 50th percentile from \code{ref_data}.
\item \code{"rel_tercile"}  — annual 33rd / 67th percentile from \code{ref_data}.
}}

\item{thresholds}{A tibble from \code{\link[=get_annual_thresholds]{get_annual_thresholds()}} with columns
\code{year}, \code{q1}, \code{q2}. Required for relative modes; \code{NULL} for absolute.}

\item{show_members}{Logical. When \code{TRUE} and \code{member_data} is supplied,
overlays individual member-country dots behind benchmark group medians.
Defaults to \code{FALSE}.}

\item{member_data}{A tibble from \code{\link[=get_cluster_member_data]{get_cluster_member_data()}} with columns
\code{country_name}, \code{year}, \code{score}, \code{group_label}, \code{country_type}. Only
used when \code{show_members = TRUE}.}
}
\value{
A \code{ggplot} object. Never \code{NULL}.
}
\description{
Produces a CLIAR-style dynamic benchmark \link[ggplot2:ggplot2-package]{ggplot2::ggplot2} visualisation.
x-axis = year (discrete character), y-axis = CTF score (0–1).
Zone bands are vertical \code{\link[ggplot2:geom_segment]{ggplot2::geom_segment()}} per year tick.
The focal country is a status-filled circle (\code{\link[ggplot2:geom_point]{ggplot2::geom_point()}}
\code{shape=21}) connected by a line. Benchmark group medians are hollow
shapes (\code{22:25}) via \code{\link[ggplot2:scale_manual]{ggplot2::scale_shape_manual()}}.
}
\details{
Returns a \code{ggplot} object — callers wrap with \code{\link[plotly:ggplotly]{plotly::ggplotly()}} for
interactive use in Shiny.
}
