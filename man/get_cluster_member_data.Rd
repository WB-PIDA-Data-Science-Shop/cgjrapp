% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_data.R
\name{get_cluster_member_data}
\alias{get_cluster_member_data}
\title{Get individual country rows belonging to selected benchmark groups}
\usage{
get_cluster_member_data(
  cluster,
  subcluster,
  score_var,
  region_codes,
  income_groups,
  year_range
)
}
\arguments{
\item{cluster}{Character. Top-level key from \code{names(cgjrdata::ctfdata_list)}.}

\item{subcluster}{Character. Second-level key.}

\item{score_var}{Character. Name of the score column to keep. For overview
cluster-score plots pass the cluster score column name (e.g.
\code{"institutional_environment_score"}); for subcluster indicator plots pass
the indicator column name.}

\item{region_codes}{Character vector of region codes. Use \code{character(0)} to
include none.}

\item{income_groups}{Character vector of income group labels. Use
\code{character(0)} to include none.}

\item{year_range}{Integer vector of length 2: \code{c(min_year, max_year)}.}
}
\value{
A tibble with columns \code{country_code}, \code{country_name}, \code{year},
\code{score} (renamed from \code{score_var}), \code{group_label}, \code{group_code},
\code{country_type} (\code{"region"} or \code{"income"}). Returns a zero-row tibble with
those columns when no groups are selected or no rows match.
}
\description{
Returns country-level rows from a subcluster tibble for every country that
belongs to one of the selected \code{region_codes} or \code{income_groups}, filtered
to the given \code{year_range}. Intended for overlaying individual dots behind
group-median benchmark lines.
}
